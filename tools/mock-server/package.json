{
  "name": "octopus-signage-mock-server",
  "version": "1.0.0",
  "description": "Mock servers for local development — playlist HTTP + MQTT command publisher",
  "private": true,
  "scripts": {
    "start": "node playlist-mock-server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "mqtt": "^5.15.0"
  }
}
